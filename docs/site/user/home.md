# User Home

`/jabali-panel/` redirects to `/jabali-panel/dashboard`. See [Dashboard](./dashboard.md) for the landing page content.

The legacy `/jabali-panel/home` route is retained as an alias for inbound links from the previous panel generation; it redirects to the dashboard with HTTP 308.
