# stalwart-cli v1.0.7 SHA-256 checksum for stalwart-cli-x86_64-unknown-linux-gnu.tar.xz
# Source: https://github.com/stalwartlabs/cli/releases/tag/v1.0.7
# Captured 2026-05-24 directly from the GitHub release artifact.
#
# The stalwart-cli tool speaks the JMAP management API and is used by
# install.sh + the panel-agent to provision Stalwart configuration as
# JMAP objects (ADR-0045 — "v0.16 management surface pivot").
#
# To capture on a version bump:
#   curl -sSL https://github.com/stalwartlabs/cli/releases/download/v<x.y.z>/stalwart-cli-x86_64-unknown-linux-gnu.tar.xz | sha256sum
# install.sh `_die`s if the placeholder PLACEHOLDER_CAPTURE_ON_FIRST_DEPLOY
# survives — same pattern as install/stalwart.sha256.
10ab8e192bb3bfec8f98bafdbb8f3bd87aa26f811514fa205b8f8c23cd0132a5  stalwart-cli-x86_64-unknown-linux-gnu.tar.xz
