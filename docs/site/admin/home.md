# Admin Home

`/jabali-admin/` redirects to `/jabali-admin/dashboard`. See [Dashboard](./dashboard.md) for the landing page content.

The legacy `/jabali-admin/home` route is retained as an alias for inbound links from the previous panel generation; it redirects to the dashboard with HTTP 308.
